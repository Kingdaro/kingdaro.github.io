// Generated by CoffeeScript 1.10.0
(function() {
  var generateProjects, projects, selector;

  selector = function(str) {
    return document.body.querySelector(str);
  };

  projects = [
    {
      name: 'project title'
    }, {
      name: 'project title'
    }, {
      name: 'project title'
    }, {
      name: 'project title'
    }, {
      name: 'project title'
    }, {
      name: 'project title'
    }, {
      name: 'project title'
    }
  ];

  generateProjects = function() {
    var div, i, len, name, results;
    div = selector('.projects');
    results = [];
    for (i = 0, len = projects.length; i < len; i++) {
      name = projects[i].name;
      results.push(div.innerHTML += "<div><h3>" + name + "</h3></div>");
    }
    return results;
  };

  window.addEventListener('load', function() {
    return generateProjects();
  });

}).call(this);
